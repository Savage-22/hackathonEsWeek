// Punto de arranque del backend.
// El bootstrap completo de Express (montaje de módulos, shared/errors,
// globalErrorHandler y pool de PostgreSQL) se implementa en la issue #3.
console.log('AgroGuardian backend — pendiente de bootstrap de Express (#3)')
